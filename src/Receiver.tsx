import React from 'react'

const LoginToggle = (props: any) => {
	return (
		<div className="receiverContainer">
			<span className="receiverName">{props.receiver}</span>
		</div>
	)
}

export default LoginToggle
